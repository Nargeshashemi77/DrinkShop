@model List<OrderViewModel>
@{
    ViewData["Title"] = $"{ViewData["OrdersType"]} Orders";
    Layout = "~/Views/Shared/AdminPanel.cshtml";
}
@{
    ulong ShowReadablePrice;
}
<div class="ec-content-wrapper">
    <div class="content">
        <div class="breadcrumb-wrapper d-flex align-items-center justify-content-between">
            <div>
                @if (ViewData["OrdersType"].Equals("pending"))
                {
                    <h1>سفارشات در حال پردازش</h1>
                }
                else if (ViewData["OrdersType"].Equals("preparation"))
                {
                    <h1>سفارشات در حال آماده سازی</h1>
                }
                else if (ViewData["OrdersType"].Equals("sending"))
                {
                    <h1>سفارشات در حال ارسال</h1>
                }
                else if (ViewData["OrdersType"].Equals("finished"))
                {
                    <h1>سفارشات انجام شده</h1>
                }
                else if (ViewData["OrdersType"].Equals("canceled"))
                {
                    <h1>سفارشات لغو شده</h1>
                }
                <p class="breadcrumbs">
                </p>
            </div>
            @*<div><a asp-controller="AddProduct" asp-action="addProduct" asp-route-message="Add" class="btn btn-primary">افزودن محصول</a></div>*@
        </div>
        <div class="row">
            <div class="col-12">
                <div class="card card-default">
                    <div class="card-body">
                        <div class="table-responsive">
                            <table id="responsive-data-table" class="table" style="width:100%">
                                <thead>
                                    <tr>
                                        <th>خریدار</th>
                                        <th>شناسه سفارش</th>
                                        <th>مبلغ</th>
                                        <th>تاریخ</th>
                                        <th>زمان</th>
                                        <th>فعالیت</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @for (int i = 0; i < Model.Count; i++)
                                    {
                                        <tr>
                                            <td>@Model[i].userName</td>
                                            <td>@Model[i].orderId</td>
                                            <td>@(((ulong)Model[i].orderPrice).ToMoreReadable())</td>
                                            <td>@Model[i].orderDate.ToShamsi()</td>
                                            <td>@Model[i].orderDate.ToString("HH:mm:ss")</td>
                                            <td>
                                                <div>
                                                    @if (ViewData["OrdersType"].Equals("pending"))
                                                    {
                                                        <button type="button" class="btn btn-outline-success dropdown-toggle dropdown-toggle-split"
                                                                data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false" data-display="static">
                                                        </button>
                                                        <div class="dropdown-menu">
                                                            <a asp-controller="Admin" asp-action="DeterminingOrderStatus" asp-route-orderId="@Model[i].orderId" asp-route-status="1" asp-route-wichOrdersList="pending" class="dropdown-item">در حال آماده سازی</a>
                                                            <a asp-controller="Admin" asp-action="DeterminingOrderStatus" asp-route-orderId="@Model[i].orderId" asp-route-status="3" asp-route-wichOrdersList="pending" class="dropdown-item">لغو سفارش</a>
                                                        </div>
                                                    }
                                                    else if (ViewData["OrdersType"].Equals("preparation"))
                                                    {
                                                        <button type="button" class="btn btn-outline-success dropdown-toggle dropdown-toggle-split"
                                                                data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false" data-display="static">
                                                        </button>
                                                        <div class="dropdown-menu">
                                                            <a asp-controller="Admin" asp-action="DeterminingOrderStatus" asp-route-orderId="@Model[i].orderId" asp-route-status="2" asp-route-wichOrdersList="reffered" class="dropdown-item">در حال ارسال</a>
                                                            <a asp-controller="Admin" asp-action="DeterminingOrderStatus" asp-route-orderId="@Model[i].orderId" asp-route-status="3" asp-route-wichOrdersList="reffered" class="dropdown-item">لغو سفارش</a>
                                                        </div>
                                                    }
                                                    else if (ViewData["OrdersType"].Equals("sending"))
                                                    {
                                                        <button type="button" class="btn btn-outline-success dropdown-toggle dropdown-toggle-split"
                                                                data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false" data-display="static">
                                                        </button>
                                                        <div class="dropdown-menu">
                                                            <a asp-controller="Admin" asp-action="DeterminingOrderStatus" asp-route-orderId="@Model[i].orderId" asp-route-status="4" asp-route-wichOrdersList="rejected" class="dropdown-item">تکمیل شده</a>
                                                            <a asp-controller="Admin" asp-action="DeterminingOrderStatus" asp-route-orderId="@Model[i].orderId" asp-route-status="3" asp-route-wichOrdersList="rejected" class="dropdown-item">لغو سفارش</a>
                                                        </div>
                                                    }
                                                </div>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

